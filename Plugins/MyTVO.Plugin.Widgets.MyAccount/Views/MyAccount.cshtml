@*@using MyTVO.Core;

    @await Component.InvokeAsync("MyAccountWidget", new { selectedTabId = MyAccountNavigationEnum.MyVehicle })*@

@model Nop.Web.Models.Customer.CustomerNavigationModel
@foreach (var item in Model.CustomerNavigationItems)
{
    <li class="@item.ItemClass @(Model.SelectedTab == item.Tab ? "active" : "inactive")">
        <a href="@Url.RouteUrl(item.RouteName)">@(item.Title)</a>
    </li>
}